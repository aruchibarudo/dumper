apiVersion: v1
kind: Service
metadata:
  name: {{ include "radar-front.fullname" . }}
  labels:
    {{- include "radar-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "radar-front.selectorLabels" . | nindent 4 }}
